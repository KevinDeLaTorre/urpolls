<%= simple_form_for @poll do |f| %>
  <%= f.input :title, label: "Title of poll:" %>
  <%= f.hidden_field :user_id, :value => current_user.id %>
  <%= f.button :submit, value: "Submit poll", class: "btn btn-primary" %>
<% end %>
